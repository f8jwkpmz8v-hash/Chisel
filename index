<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<meta name="theme-color" content="#111827">
<title>AMRAP 20 — Personal Workout</title>
<style>
  :root{
    --bg:#0b0f14; --panel:#121821; --panel2:#18202b; --text:#f4f7fb;
    --muted:#9aa6b5; --accent:#62e6a8; --accent2:#39c98a; --danger:#ff6b6b;
    --line:#263140; --shadow:0 16px 40px rgba(0,0,0,.28);
  }
  *{box-sizing:border-box}
  body{margin:0;background:var(--bg);color:var(--text);font-family:Inter,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}
  button{font:inherit}
  .app{max-width:1050px;margin:auto;padding:20px 16px 44px}
  header{display:flex;justify-content:space-between;align-items:center;gap:16px;margin-bottom:22px}
  .brand{display:flex;align-items:center;gap:12px}
  .logo{width:44px;height:44px;border-radius:13px;background:var(--accent);color:#07110c;display:grid;place-items:center;font-weight:900}
  h1,h2,h3,p{margin:0}
  h1{font-size:22px;letter-spacing:-.5px}
  .sub{color:var(--muted);font-size:13px;margin-top:3px}
  .streak{background:var(--panel);border:1px solid var(--line);padding:9px 12px;border-radius:12px;font-size:13px}
  .streak b{color:var(--accent)}
  nav{display:grid;grid-template-columns:repeat(3,1fr);gap:8px;background:var(--panel);padding:6px;border-radius:15px;margin-bottom:20px}
  nav button{border:0;background:transparent;color:var(--muted);padding:11px 8px;border-radius:10px;cursor:pointer;font-weight:700}
  nav button.active{background:var(--panel2);color:var(--text)}
  .view{display:none}.view.active{display:block}
  .hero{background:linear-gradient(145deg,#151e29,#10161f);border:1px solid var(--line);border-radius:20px;padding:22px;box-shadow:var(--shadow);margin-bottom:16px}
  .eyebrow{font-size:12px;text-transform:uppercase;letter-spacing:1.5px;color:var(--accent);font-weight:800}
  .hero h2{font-size:32px;letter-spacing:-1px;margin:6px 0}
  .hero p{color:var(--muted);max-width:650px;line-height:1.5}
  .day-type-controls{display:grid;grid-template-columns:repeat(5,1fr);gap:8px;margin-top:10px}
  .day-type{border:1px solid var(--line);background:var(--panel);color:var(--muted);border-radius:14px;padding:13px 8px;cursor:pointer;font-weight:800;min-height:72px}
  .day-type .icon{display:block;font-size:25px;line-height:1;margin-bottom:7px}
  .day-type.selected{border-color:var(--accent);background:#173127;color:var(--text)}
  .special-day{background:var(--panel);border:1px solid var(--line);border-radius:18px;padding:22px;text-align:center}
  .special-day .big-icon{font-size:48px;margin-bottom:8px}
  .special-day h3{font-size:22px;margin-bottom:6px}
  .special-day p{color:var(--muted);line-height:1.5;margin-bottom:16px}
  .controls{display:flex;flex-wrap:wrap;gap:8px;margin-top:18px}
  .chip{border:1px solid var(--line);background:var(--panel);color:var(--muted);border-radius:999px;padding:8px 13px;cursor:pointer}
  .chip.selected{border-color:var(--accent);color:var(--text);background:#173127}
  .primary{border:0;background:var(--accent);color:#06120c;font-weight:900;border-radius:12px;padding:13px 18px;cursor:pointer}
  .secondary{border:1px solid var(--line);background:var(--panel);color:var(--text);border-radius:12px;padding:12px 16px;cursor:pointer}
  .section-title{display:flex;justify-content:space-between;align-items:center;margin:22px 0 10px}
  .section-title span{color:var(--muted);font-size:13px}
  .workout-list{display:grid;grid-template-columns:repeat(2,1fr);gap:10px}
  .exercise{background:var(--panel);border:1px solid var(--line);border-radius:14px;padding:15px;display:flex;gap:12px;align-items:center}
  .num{width:30px;height:30px;border-radius:9px;background:var(--panel2);display:grid;place-items:center;color:var(--accent);font-weight:900}
  .exercise strong{display:block}.exercise small{display:block;color:var(--muted);margin-top:3px}
  .badge{margin-left:auto;color:var(--muted);font-size:11px;border:1px solid var(--line);border-radius:999px;padding:5px 8px}
  .actions{display:flex;gap:10px;margin-top:16px}
  .timer{background:#050809;border:1px solid #202830;border-radius:22px;padding:28px;text-align:center;box-shadow:var(--shadow)}
  .timer .label{color:var(--accent);font-weight:900;letter-spacing:2px;font-size:13px}
  .clock{font-size:78px;font-variant-numeric:tabular-nums;font-weight:900;letter-spacing:-4px;margin:10px 0 16px}
  .timer .round{color:var(--muted);margin-bottom:18px}
  .timer-buttons{display:flex;justify-content:center;gap:10px;flex-wrap:wrap}
  .timer-buttons button{min-width:110px}
  .calendar-card{background:var(--panel);border:1px solid var(--line);border-radius:18px;padding:18px}
  .calendar-head{display:flex;justify-content:space-between;align-items:center;margin-bottom:14px}
  .calendar-head button{border:1px solid var(--line);background:var(--panel2);color:var(--text);border-radius:9px;width:34px;height:34px;cursor:pointer}
  .week,.days{display:grid;grid-template-columns:repeat(7,1fr);gap:6px}
  .week div{text-align:center;color:var(--muted);font-size:11px;padding:5px}
  .day{aspect-ratio:1;border:1px solid var(--line);border-radius:9px;display:grid;place-items:center;font-size:12px;color:var(--muted)}
  .day.done{background:#173127;color:var(--accent);border-color:#2b7254;font-weight:900}
  .day.today{outline:2px solid var(--accent);outline-offset:1px}
  .day.empty{border-color:transparent}
  .stats{display:grid;grid-template-columns:repeat(3,1fr);gap:10px;margin-top:12px}
  .stat{background:var(--panel2);border-radius:13px;padding:14px}.stat b{display:block;font-size:24px}.stat span{color:var(--muted);font-size:12px}
  .tabata-grid{display:grid;grid-template-columns:1fr 1fr;gap:12px}
  .setting{background:var(--panel);border:1px solid var(--line);border-radius:14px;padding:15px}
  .setting label{display:block;color:var(--muted);font-size:12px;margin-bottom:7px}
  input,select{width:100%;background:var(--panel2);color:var(--text);border:1px solid var(--line);border-radius:9px;padding:11px}
  .tabata-timer{margin-top:14px}
  .phase{font-size:26px;font-weight:900}
  .small-clock{font-size:64px}
  .exercise-pills{display:flex;flex-wrap:wrap;gap:7px;margin-top:14px}
  .exercise-pills span{background:var(--panel2);border:1px solid var(--line);padding:7px 10px;border-radius:9px;color:var(--muted);font-size:12px}
  @media(max-width:700px){
    .workout-list,.tabata-grid{grid-template-columns:1fr}
    .day-type-controls{grid-template-columns:repeat(3,1fr)}
    .clock{font-size:64px}.hero h2{font-size:27px}
  }
</style>
</head>
<body>
<div class="app">
  <header>
    <div class="brand"><div class="logo">20</div><div><h1>AMRAP 20</h1><div class="sub">Your personal full-body training app</div></div></div>
    <div class="streak">🔥 <b id="headerStreak">0</b> day streak</div>
  </header>

  <nav>
    <button class="active" data-view="workouts">Workouts</button>
    <button data-view="warmup">Warmup</button>
    <button data-view="calendar">Calendar</button>
  </nav>

  <main>
    <section id="workouts" class="view active">
      <div class="hero">
        <div class="eyebrow">20-minute AMRAP</div>
        <h2 id="focusTitle">Full Body</h2>
        <p>Six movements. Full-body coverage. Choose the focus for today's workout, or mark today as a rest or cardio day.</p>
        <div class="section-title"><h3>What are you training today?</h3><span>Select one</span></div>
        <div class="day-type-controls" id="dayTypeControls"></div>
        <div id="amrapBuilder">
        <div class="section-title"><h3>Workout focus</h3></div>
        <div class="controls" id="focusControls"></div>
        <div class="section-title"><h3>Equipment</h3><span>Choose what you have</span></div>
        <div class="controls" id="equipmentControls"></div>
        <div class="actions"><button class="primary" id="generate">Generate Workout</button><button class="secondary" id="startWorkout">Start 20:00 AMRAP</button></div>
        </div>
      </div>

      <div class="section-title"><h3>Today's Workout</h3><span id="workoutSubtitle">As many rounds + reps as possible</span></div>
      <div class="workout-list" id="workoutList"></div>

      <div id="timerPanel" style="display:none;margin-top:16px">
        <div class="timer">
          <div class="label">AMRAP</div>
          <div class="clock" id="amrapClock">20:00</div>
          <div class="round">Keep moving • record your final rounds + reps</div>
          <div class="timer-buttons">
            <button class="primary" id="pauseAmrap">Pause</button>
            <button class="secondary" id="resetAmrap">Reset</button>
            <button class="secondary" id="finishAmrap">Finish Workout</button>
          </div>
        </div>
      </div>
    </section>

    <section id="warmup" class="view">
      <div class="hero">
        <div class="eyebrow">Custom warmup</div>
        <h2>Tabata Timer</h2>
        <p>Build your own warmup intervals before the AMRAP. Defaults to classic 20 seconds work / 10 seconds rest.</p>
      </div>
      <div class="tabata-grid">
        <div class="setting"><label>Work — seconds</label><input id="workSec" type="number" min="5" max="120" value="20"></div>
        <div class="setting"><label>Rest — seconds</label><input id="restSec" type="number" min="0" max="120" value="10"></div>
        <div class="setting"><label>Rounds</label><input id="rounds" type="number" min="1" max="30" value="8"></div>
        <div class="setting"><label>Exercises (optional)</label><input id="tabataExercises" placeholder="Squats, Push-ups, Lunges..."></div>
      </div>
      <div class="timer tabata-timer">
        <div class="phase" id="tabataPhase">READY</div>
        <div class="clock small-clock" id="tabataClock">00:20</div>
        <div class="round" id="tabataRound">Round 1 of 8</div>
        <div class="exercise-pills" id="tabataPills"></div>
        <div class="timer-buttons" style="margin-top:18px">
          <button class="primary" id="startTabata">Start</button>
          <button class="secondary" id="pauseTabata">Pause</button>
          <button class="secondary" id="resetTabata">Reset</button>
        </div>
      </div>
    </section>

    <section id="calendar" class="view">
      <div class="hero">
        <div class="eyebrow">Consistency</div>
        <h2>Training Calendar</h2>
        <p>Every completed AMRAP is saved locally in this browser and contributes to your streak.</p>
      </div>
      <div class="calendar-card">
        <div class="calendar-head"><button id="prevMonth">‹</button><h3 id="monthTitle"></h3><button id="nextMonth">›</button></div>
        <div class="week"><div>Sun</div><div>Mon</div><div>Tue</div><div>Wed</div><div>Thu</div><div>Fri</div><div>Sat</div></div>
        <div class="days" id="days"></div>
      </div>
      <div class="stats">
        <div class="stat"><b id="currentStreak">0</b><span>Current streak</span></div>
        <div class="stat"><b id="longestStreak">0</b><span>Longest streak</span></div>
        <div class="stat"><b id="totalWorkouts">0</b><span>Total workouts</span></div>
      </div>
    </section>
  </main>
</div>

<script>
const DB = {
  bodyweight: [
    {n:"Push-Ups",e:"Bodyweight",f:["chest","arms"]},{n:"Mountain Climbers",e:"Bodyweight",f:["core","legs"]},
    {n:"Air Squats",e:"Bodyweight",f:["legs"]},{n:"Reverse Lunges",e:"Bodyweight",f:["legs"]},
    {n:"V-Ups",e:"Bodyweight",f:["core"]},{n:"Burpees",e:"Bodyweight",f:["chest","legs","core"]},
    {n:"Plank Shoulder Taps",e:"Bodyweight",f:["core","shoulders"]},{n:"Jump Squats",e:"Bodyweight",f:["legs"]}
  ],
  trx: [
    {n:"TRX Rows",e:"TRX",f:["back","arms"]},{n:"TRX Chest Press",e:"TRX",f:["chest","arms"]},
    {n:"TRX Squats",e:"TRX",f:["legs"]},{n:"TRX Reverse Lunges",e:"TRX",f:["legs"]},
    {n:"TRX Knee Tucks",e:"TRX",f:["core"]},{n:"TRX Face Pulls",e:"TRX",f:["back","shoulders"]},
    {n:"TRX Biceps Curls",e:"TRX",f:["arms"]},{n:"TRX Triceps Extensions",e:"TRX",f:["arms"]}
  ],
  dumbbells: [
    {n:"DB Push Press",e:"Dumbbells",f:["shoulders","arms"]},{n:"DB Romanian Deadlift",e:"Dumbbells",f:["legs","back"]},
    {n:"Single-Arm DB Row",e:"Dumbbells",f:["back","arms"]},{n:"DB Goblet Squat",e:"Dumbbells",f:["legs"]},
    {n:"DB Reverse Lunge",e:"Dumbbells",f:["legs"]},{n:"DB Renegade Row",e:"Dumbbells",f:["back","core"]},
    {n:"DB Thrusters",e:"Dumbbells",f:["legs","shoulders"]},{n:"DB Hammer Curls",e:"Dumbbells",f:["arms"]}
  ],
  kettlebell: [
    {n:"KB Swings",e:"Kettlebell",f:["legs","back"]},{n:"KB Goblet Squat",e:"Kettlebell",f:["legs"]},
    {n:"KB Clean & Press",e:"Kettlebell",f:["shoulders","arms"]},{n:"KB Russian Twists",e:"Kettlebell",f:["core"]},
    {n:"KB Deadlift",e:"Kettlebell",f:["legs","back"]},{n:"KB Gorilla Row",e:"Kettlebell",f:["back","arms"]},
    {n:"KB Front Rack Reverse Lunge",e:"Kettlebell",f:["legs","core"]},{n:"KB Farmer Carry",e:"Kettlebell",f:["core","back"]}
  ]
};

const focusNames={all:"Full Body",arms:"Arms",legs:"Legs",back:"Back",core:"Core"};
const dayTypes={
  arms:{icon:"💪",label:"Arms",desc:"Upper-body emphasis with 2–3 arm-focused movements."},
  legs:{icon:"🦵",label:"Legs",desc:"Lower-body emphasis with 2–3 leg-focused movements."},
  back:{icon:"🔙",label:"Back",desc:"Back and pulling emphasis with full-body support."},
  core:{icon:"🔥",label:"Core",desc:"Core emphasis with 2–3 trunk-focused movements."},
  rest:{icon:"😴",label:"Rest Day",desc:"Recovery day. No AMRAP — take the day off and recover."},
  cardio:{icon:"❤️",label:"Cardio Day",desc:"Cardio-focused day. No strength AMRAP — keep it aerobic."}
};
let dayType="arms";
let focus="arms";
let equipment=new Set(["bodyweight","trx","dumbbells","kettlebell"]);
let workout=[];

function renderDayTypes(){
  const box=document.getElementById("dayTypeControls");
  box.innerHTML=Object.entries(dayTypes).map(([k,v])=>
    `<button class="day-type ${k===dayType?"selected":""}" data-daytype="${k}">
      <span class="icon">${v.icon}</span>${v.label}
    </button>`).join("");
  box.querySelectorAll("[data-daytype]").forEach(b=>b.onclick=()=>{
    dayType=b.dataset.daytype;
    focus=dayType==="rest"||dayType==="cardio"?"all":dayType;
    renderDayTypes();
    updateDayMode();
  });
}
function updateDayMode(){
  const builder=document.getElementById("amrapBuilder");
  const list=document.getElementById("workoutList");
  const subtitle=document.getElementById("workoutSubtitle");
  const title=document.getElementById("focusTitle");
  if(dayType==="rest"||dayType==="cardio"){
    builder.style.display="none";
    document.getElementById("timerPanel").style.display="none";
    title.textContent=dayTypes[dayType].label;
    subtitle.textContent=dayTypes[dayType].label;
    list.innerHTML=`<div class="special-day" style="grid-column:1/-1">
      <div class="big-icon">${dayTypes[dayType].icon}</div>
      <h3>${dayTypes[dayType].label}</h3>
      <p>${dayTypes[dayType].desc}</p>
      <button class="primary" onclick="markSpecialDay('${dayType}')">Mark Today as ${dayTypes[dayType].label}</button>
    </div>`;
  }else{
    builder.style.display="block";
    title.textContent=focusNames[focus];
    subtitle.textContent="As many rounds + reps as possible";
    buildWorkout();
  }
}
function markSpecialDay(type){
  let dates=JSON.parse(localStorage.getItem("amrapDates")||"[]");
  let key=iso(new Date());
  dates=dates.filter(x=>x.date!==key);
  dates.push({date:key,type});
  localStorage.setItem("amrapDates",JSON.stringify(dates));
  updateStreak();
  alert(`${dayTypes[type].label} saved for today.`);
}
function sample(arr,n){return [...arr].sort(()=>Math.random()-.5).slice(0,n)}
function buildWorkout(){
  let pool=[...equipment].flatMap(k=>DB[k]||[]);
  if(pool.length<6){alert("Select at least enough equipment to provide six exercises.");return}
  const chosen=[];
  const used=new Set();
  const add=(x)=>{if(x && !used.has(x.n)){chosen.push(x);used.add(x.n);return true}return false};
  if(focus!=="all"){
    sample(pool.filter(x=>x.f.includes(focus)),3).forEach(add);
  }
  const priorities = focus==="all" ? ["legs","back","chest","core","arms","shoulders"] : ["legs","back","chest","core","arms","shoulders"].filter(x=>x!==focus);
  for(const p of priorities){
    if(chosen.length>=6) break;
    add(sample(pool.filter(x=>x.f.includes(p)),1)[0]);
  }
  sample(pool.filter(x=>!used.has(x.n)),6-chosen.length).forEach(add);
  workout=chosen.slice(0,6);
  renderWorkout();
}
function reps(x){
  if(x.f.includes("legs") && x.f.includes("back")) return 15;
  if(x.f.includes("core")) return 12;
  return 10;
}
function renderWorkout(){
  document.getElementById("focusTitle").textContent=focusNames[focus];
  document.getElementById("workoutList").innerHTML=workout.map((x,i)=>
    `<div class="exercise"><div class="num">${i+1}</div><div><strong>${reps(x)} ${x.n}</strong><small>${x.f.map(f=>f[0].toUpperCase()+f.slice(1)).join(" • ")}</small></div><div class="badge">${x.e}</div></div>`
  ).join("");
}
function setupControls(){
  const fc=document.getElementById("focusControls");
  fc.innerHTML=Object.entries(focusNames).map(([k,v])=>`<button class="chip ${k===focus?"selected":""}" data-focus="${k}">${v}</button>`).join("");
  fc.querySelectorAll("[data-focus]").forEach(b=>b.onclick=()=>{focus=b.dataset.focus;setupControls();buildWorkout()});
  const ec=document.getElementById("equipmentControls");
  ec.innerHTML=Object.entries({bodyweight:"Bodyweight",trx:"TRX",dumbbells:"Dumbbells",kettlebell:"Kettlebell"}).map(([k,v])=>`<button class="chip ${equipment.has(k)?"selected":""}" data-eq="${k}">${v}</button>`).join("");
  ec.querySelectorAll("[data-eq]").forEach(b=>b.onclick=()=>{equipment.has(b.dataset.eq)?equipment.delete(b.dataset.eq):equipment.add(b.dataset.eq);setupControls();buildWorkout()});
}
document.querySelectorAll("nav button").forEach(b=>b.onclick=()=>{document.querySelectorAll("nav button").forEach(x=>x.classList.remove("active"));b.classList.add("active");document.querySelectorAll(".view").forEach(x=>x.classList.remove("active"));document.getElementById(b.dataset.view).classList.add("active");if(b.dataset.view==="calendar")renderCalendar()});
document.getElementById("generate").onclick=buildWorkout;

let amrapInterval=null, amrapLeft=1200, amrapRunning=false;
function drawAmrap(){let m=Math.floor(amrapLeft/60),s=amrapLeft%60;document.getElementById("amrapClock").textContent=`${String(m).padStart(2,"0")}:${String(s).padStart(2,"0")}`}
document.getElementById("startWorkout").onclick=()=>{document.getElementById("timerPanel").style.display="block";amrapLeft=1200;amrapRunning=true;drawAmrap();clearInterval(amrapInterval);amrapInterval=setInterval(()=>{if(amrapRunning){amrapLeft--;drawAmrap();if(amrapLeft<=0){clearInterval(amrapInterval);amrapRunning=false;completeWorkout()}}},1000)};
document.getElementById("pauseAmrap").onclick=()=>{amrapRunning=!amrapRunning;document.getElementById("pauseAmrap").textContent=amrapRunning?"Pause":"Resume"};
document.getElementById("resetAmrap").onclick=()=>{clearInterval(amrapInterval);amrapRunning=false;amrapLeft=1200;drawAmrap();document.getElementById("pauseAmrap").textContent="Pause"};
document.getElementById("finishAmrap").onclick=()=>{clearInterval(amrapInterval);amrapRunning=false;completeWorkout()};
function completeWorkout(){
  let dates=JSON.parse(localStorage.getItem("amrapDates")||"[]");
  let today=iso(new Date());
  dates=dates.map(x=>typeof x==="string"?{date:x,type:"workout"}:x);
  if(!dates.some(x=>x.date===today)) dates.push({date:today,type:"workout"});
  localStorage.setItem("amrapDates",JSON.stringify(dates));
  document.getElementById("timerPanel").style.display="none";
  updateStreak();
}

let tabataInterval=null, tabataRunning=false, tabataPhase="work", tabataLeft=20, tabataRoundNum=1;
function tabataSettings(){return {work:+document.getElementById("workSec").value||20,rest:+document.getElementById("restSec").value||10,rounds:+document.getElementById("rounds").value||8}}
function drawTabata(){let m=Math.floor(tabataLeft/60),s=tabataLeft%60;document.getElementById("tabataClock").textContent=`${String(m).padStart(2,"0")}:${String(s).padStart(2,"0")}`;document.getElementById("tabataPhase").textContent=tabataPhase==="work"?"WORK":"REST";document.getElementById("tabataRound").textContent=`Round ${Math.min(tabataRoundNum,tabataSettings().rounds)} of ${tabataSettings().rounds}`}
function resetTabata(){clearInterval(tabataInterval);tabataRunning=false;let z=tabataSettings();tabataPhase="work";tabataLeft=z.work;tabataRoundNum=1;drawTabata()}
document.getElementById("startTabata").onclick=()=>{if(tabataRunning)return;tabataRunning=true;clearInterval(tabataInterval);tabataInterval=setInterval(()=>{tabataLeft--;if(tabataLeft<=0){let z=tabataSettings();if(tabataPhase==="work"){tabataPhase="rest";tabataLeft=z.rest;if(z.rest===0){tabataPhase="work";tabataRoundNum++}}else{tabataPhase="work";tabataRoundNum++}if(tabataRoundNum>z.rounds){clearInterval(tabataInterval);tabataRunning=false;tabataPhase="work";tabataRoundNum=1;tabataLeft=z.work}}drawTabata()},1000)};
document.getElementById("pauseTabata").onclick=()=>{tabataRunning=!tabataRunning};
document.getElementById("resetTabata").onclick=resetTabata;
["workSec","restSec","rounds"].forEach(id=>document.getElementById(id).onchange=resetTabata);
document.getElementById("tabataExercises").oninput=()=>{let vals=document.getElementById("tabataExercises").value.split(",").map(x=>x.trim()).filter(Boolean);document.getElementById("tabataPills").innerHTML=vals.map(x=>`<span>${x}</span>`).join("")};

let calendarDate=new Date();
function iso(d){return new Date(d.getFullYear(),d.getMonth(),d.getDate()).toISOString().slice(0,10)}
function getEntries(){
  let raw=JSON.parse(localStorage.getItem("amrapDates")||"[]");
  return raw.map(x=>typeof x==="string"?{date:x,type:"workout"}:x);
}
function renderCalendar(){
 const y=calendarDate.getFullYear(),m=calendarDate.getMonth(), first=new Date(y,m,1), last=new Date(y,m+1,0), entries=getEntries();
 const map=new Map(entries.map(x=>[x.date,x.type]));
 document.getElementById("monthTitle").textContent=new Intl.DateTimeFormat(undefined,{month:"long",year:"numeric"}).format(calendarDate);
 let out="";for(let i=0;i<first.getDay();i++)out+='<div class="day empty"></div>';
 for(let d=1;d<=last.getDate();d++){
   let date=new Date(y,m,d), key=iso(date), type=map.get(key), cls="day";
   if(type)cls+=" done";
   if(key===iso(new Date()))cls+=" today";
   const icon=type==="rest"?"😴":type==="cardio"?"❤️":type?"✓":"";
   out+=`<div class="${cls}" title="${type?dayTypes[type]?.label||"Workout":""}">${icon||d}</div>`;
 }
 document.getElementById("days").innerHTML=out;updateStreak();
}
document.getElementById("prevMonth").onclick=()=>{calendarDate.setMonth(calendarDate.getMonth()-1);renderCalendar()};
document.getElementById("nextMonth").onclick=()=>{calendarDate.setMonth(calendarDate.getMonth()+1);renderCalendar()};
function streaks(){
 let entries=getEntries().sort((a,b)=>a.date.localeCompare(b.date)), set=new Set(entries.map(x=>x.date)), today=new Date();
 let cur=0,d=new Date();while(set.has(iso(d))){cur++;d.setDate(d.getDate()-1)}
 let longest=0,run=0,prev=null;
 entries.forEach(x=>{
   let d=new Date(x.date+"T00:00:00");
   if(prev && (d-prev)===86400000)run++;else run=1;
   longest=Math.max(longest,run);prev=d;
 });
 const workoutCount=entries.filter(x=>x.type==="workout").length;
 return {cur,longest,total:workoutCount};
}
function updateStreak(){
 let s=streaks();
 document.getElementById("headerStreak").textContent=s.cur;
 document.getElementById("currentStreak").textContent=s.cur;
 document.getElementById("longestStreak").textContent=s.longest;
 document.getElementById("totalWorkouts").textContent=s.total;
}

renderDayTypes();setupControls();buildWorkout();resetTabata();renderCalendar();updateDayMode();
</script>
</body>
</html>
